<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <record id="action_kpi_aktual_qweb" model="ir.actions.act_url">
        <field name="name">Input Ketercapaian KPI</field>
        <field name="url">/bootcamp/kpi/aktual</field>
        <field name="target">self</field>
    </record>

    <template id="template_kpi_aktual" name="Input KPI Aktual Bootcamp Fapet">
        <t t-call="web.layout">
            <t t-set="head">
                <style>
                    * { box-sizing: border-box; margin: 0; padding: 0; }

                    body {
                        font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
                        background: #f5f5f5;
                        display: flex;
                        min-height: 100vh;
                    }

                    /* ========== CSS KONTEN UTAMA KPI AKTUAL ========== */
                    .main-content {
                        margin-left: 240px; 
                        flex: 1;
                        width: calc(100% - 240px);
                        padding: 32px;
                        min-height: 100vh;
                    }
                    .topbar {
                        display: flex; justify-content: space-between; align-items: flex-start;
                        margin-bottom: 28px;
                    }
                    .page-title { font-size: 1.6rem; font-weight: 700; color: #1a1a2e; }
                    .page-subtitle { font-size: 0.875rem; color: #888; margin-top: 4px; }
                    .topbar-date { font-size: 0.875rem; color: #888; }

                    /* Layout Grid 2 Kolom */
                    .input-layout {
                        display: grid; grid-template-columns: 1fr 1fr; gap: 24px;
                        width: 100%;
                    }
                    
                    .panel-card {
                        background: #fff; border-radius: 12px; padding: 24px;
                        box-shadow: 0 1px 4px rgba(0,0,0,0.06); align-self: start;
                    }
                    .panel-title {
                        font-size: 1.1rem; font-weight: 700; color: #1a1a2e;
                        margin-bottom: 24px; display: flex; align-items: center; gap: 8px;
                    }

                    /* Form Elements */
                    .form-group { margin-bottom: 20px; }
                    .form-label { 
                        display: block; font-size: 0.875rem; font-weight: 600; 
                        color: #1a1a2e; margin-bottom: 8px; 
                    }
                    .form-control { 
                        width: 100%; padding: 12px 14px; border: 1px solid #e5e7eb; 
                        border-radius: 8px; font-size: 0.875rem; transition: border-color 0.15s; 
                        color: #333; background: #fff;
                    }
                    .form-control:focus { outline: none; border-color: #7c3aed; box-shadow: 0 0 0 3px rgba(124,58,237,0.1); }
                    .btn-submit { 
                        background: #6d4c41; color: #fff; border: none; border-radius: 8px; 
                        padding: 14px 24px; font-size: 0.9rem; font-weight: 600; 
                        cursor: pointer; width: 100%; margin-top: 10px; 
                        display: flex; justify-content: center; align-items: center; gap: 8px;
                        transition: background 0.15s;
                    }
                    .btn-submit:hover { background: #5d4037; }

                    /* Daftar Entri Terbaru */
                    .entry-item {
                        padding: 16px; border: 1px solid #f3f4f6; border-radius: 8px; 
                        margin-bottom: 12px; display: flex; justify-content: space-between; align-items: center;
                    }
                    .entry-item:last-child { margin-bottom: 0; }
                    .entry-title { font-weight: 600; font-size: 0.95rem; color: #1a1a2e; margin-bottom: 4px; }
                    .entry-meta { font-size: 0.8rem; color: #888; }
                    .entry-value-wrap { text-align: right; }
                    .entry-value { font-weight: 700; color: #7c3aed; font-size: 1rem; margin-bottom: 4px; }
                    .badge-success { font-size: 0.7rem; background: #dcfce7; color: #166534; padding: 3px 8px; border-radius: 12px; font-weight: 600; }
                    .badge-warning { font-size: 0.7rem; background: #fef08a; color: #b45309; padding: 3px 8px; border-radius: 12px; font-weight: 600; }
                </style>
            </t>

            <div style="display:flex; min-height:100vh; width:100%;">

                <t t-call="bootcamp_fapet.component_sidebar"/>

                <div class="main-content">
                    <div class="topbar">
                        <div>
                            <div class="page-title">Input Ketercapaian KPI</div>
                            <div class="page-subtitle">Form input dan pembaruan nilai aktual KPI divisi</div>
                        </div>
                        <div class="topbar-date" id="current-date"/>
                    </div>

                    <div class="input-layout">
                        
                        <div class="panel-card">
                            <div class="panel-title">
                                <i class="fa fa-file-text-o" style="color:#7c3aed;"/> Form Input Aktual
                            </div>
                            
                            <form action="/bootcamp/kpi/aktual/submit" method="post">
                                <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                                
                                <div class="form-group">
                                    <label class="form-label"><i class="fa fa-calendar-o" style="color:#888; margin-right:4px;"/> Tanggal</label>
                                    <input type="date" name="tanggal" class="form-control" required="required" id="input-tanggal"/>
                                </div>
                                
                                <div class="form-group">
                                    <label class="form-label">Divisi</label>
                                    <select name="divisi" class="form-control" required="required" id="select-divisi">
                                        <option value="">Pilih Divisi...</option>
                                        <option value="Produksi">Produksi</option>
                                        <option value="Keuangan">Keuangan</option>
                                        <option value="Processing">Processing</option>
                                        <option value="IT">IT</option>
                                        <option value="HR">HR</option>
                                        <option value="Marketing">Marketing</option>
                                    </select>
                                </div>

                                <div class="form-group">
                                    <label class="form-label">Indikator KPI Target</label>
                                    <select name="kpi_target_id" class="form-control" required="required" id="select-indikator">
                                        <option value="">Pilih Indikator...</option>
                                        <t t-foreach="kpi_targets" t-as="target">
                                            <option t-att-value="target.id" t-att-data-divisi="target.divisi">
                                                <t t-esc="target.indikator"/> (Target: <t t-esc="target.target_nilai"/>)
                                            </option>
                                        </t>
                                    </select>
                                </div>

                                <div class="form-group">
                                    <label class="form-label"><i class="fa fa-calculator" style="color:#888; margin-right:4px;"/> Nilai Aktual</label>
                                    <input type="number" step="0.01" name="nilai_aktual" class="form-control" placeholder="Masukkan nilai pencapaian..." required="required"/>
                                </div>

                                <button type="submit" class="btn-submit">
                                    <i class="fa fa-save"/> Simpan Data Aktual
                                </button>
                            </form>
                        </div>

                        <div class="panel-card">
                            <div class="panel-title">Entri Terbaru</div>
                            <div>
                                <t t-if="not kpi_aktual_list">
                                    <div style="text-align:center; color:#888; padding:30px 0; font-size:0.9rem;">
                                        Belum ada entri pencapaian KPI.
                                    </div>
                                </t>
                                <t t-foreach="kpi_aktual_list" t-as="aktual">
                                    <div class="entry-item">
                                        <div>
                                            <div class="entry-title"><t t-esc="aktual.kpi_target_id.indikator"/></div>
                                            <div class="entry-meta">
                                                <t t-esc="aktual.tanggal.strftime('%A, %d %B %Y')"/> • Divisi <t t-esc="aktual.divisi"/>
                                            </div>
                                        </div>
                                        <div class="entry-value-wrap">
                                            <div class="entry-value"><t t-esc="aktual.nilai_aktual"/></div>
                                            <t t-if="aktual.status == 'tercapai'">
                                                <span class="badge-success">Memenuhi Target</span>
                                            </t>
                                            <t t-else="">
                                                <span class="badge-warning">Belum Memenuhi</span>
                                            </t>
                                        </div>
                                    </div>
                                </t>
                            </div>
                        </div>

                    </div>
                </div>
            </div>

            <script>
                var d = new Date();
                document.getElementById('input-tanggal').valueAsDate = d;

                var days = ['Minggu','Senin','Selasa','Rabu','Kamis','Jumat','Sabtu'];
                var months = ['Januari','Februari','Maret','April','Mei','Juni','Juli','Agustus','September','Oktober','November','Desember'];
                var el = document.getElementById('current-date');
                if (el) el.textContent = days[d.getDay()] + ', ' + d.getDate() + ' ' + months[d.getMonth()] + ' ' + d.getFullYear();

                document.getElementById('select-divisi').addEventListener('change', function() {
                    var selectedDivisi = this.value;
                    var optionIndikator = document.querySelectorAll('#select-indikator option');
                    
                    optionIndikator.forEach(function(opt) {
                        if (!opt.value) return; 
                        if (opt.dataset.divisi === selectedDivisi || !selectedDivisi) {
                            opt.style.display = ''; 
                        } else {
                            opt.style.display = 'none'; 
                        }
                    });
                    document.getElementById('select-indikator').value = '';
                });
            </script>
        </t>
    </template>
</odoo>